{
  "name": "sistema-web",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix servidor\" \"npm run dev --prefix cliente\"",
    "instalar-todo": "npm install && npm install --prefix cliente && npm install --prefix servidor"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}